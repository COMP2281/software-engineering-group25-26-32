All tests were performed on a system running Windows 11 version 23H2, with Python Version 3.13.7 and NodeJS Version 24.8.0.
System Testing was performed on a range of popular web browsers, including Google Chrome, Firefox, Safari and Microsoft Edge. Results were identical across all browsers tested and system functionality should be the same with all modern desktop browsers.

For each test in previous section, need to justify severity score (how bad it would be if the test fails), and explain what each score means.

# Installing Ollama 
- Go to `https://ollama.com/download` and follow the instructions for your operating system
- Verify the installation was successful by opening a new terminal and running `ollama --version`
- - if this fails, restart your computer
- Install the Mistral model using `ollama pull mistral`
- Ensure the `ollama` Python module is installed (it is in requirements.txt)